{
  "name": "claude-channel-robotomail",
  "version": "0.3.0",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "[ -d node_modules ] || bun install --no-summary; bun server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
